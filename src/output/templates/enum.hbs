{{#if description}}
  /** {{description}} */
{{/if}}
  enum {{name}} {
{{#each values}}
{{#if description}}
    /** {{description}} */
{{/if}}    {{name}}{{#if resolvedValue}} = {{{resolvedValue}}}{{/if}}{{#unless @last}},{{/unless}}
{{/each}}
  }
